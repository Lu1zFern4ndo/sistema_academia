<%= form_with(model: pagamento) do |form| %>
  <% if pagamento.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pagamento.errors.count, "error") %> prohibited this pagamento from being saved:</h2>

      <ul>
        <% pagamento.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data_pagamento, style: "display: block" %>
    <%= form.date_field :data_pagamento %>
  </div>

  <div>
    <%= form.label :valor_pago, style: "display: block" %>
    <%= form.text_field :valor_pago %>
  </div>

  <div>
    <%= form.label :aluno_id, style: "display: block" %>
    <%= form.text_field :aluno_id %>
  </div>

  <div>
    <%= form.label :atividade_fisica_id, style: "display: block" %>
    <%= form.text_field :atividade_fisica_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
